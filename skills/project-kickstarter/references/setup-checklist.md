# Setup Checklist by Template

Complete step-by-step commands and file creation for each project template. Every command is listed in order. Run them sequentially.

---

## SaaS Template (default)

### Phase 1: Create and Configure Project

```bash
# 1. Create Next.js project
npx create-next-app@latest PROJECT_NAME \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --src-dir=false \
  --import-alias="@/*" \
  --use-npm

# 2. Enter project directory
cd PROJECT_NAME

# 3. Initialize git (create-next-app does this, but ensure it's clean)
git init

# 4. Install Supabase packages
npm install @supabase/supabase-js @supabase/ssr

# 5. Install Stripe
npm install stripe @stripe/stripe-js

# 6. Install Gemini AI SDK
npm install @google/generative-ai

# 7. Install UI utilities
npm install lucide-react class-variance-authority clsx tailwind-merge

# 8. Install dev tools
npm install -D prettier prettier-plugin-tailwindcss @types/node

# 9. Initialize shadcn/ui (uses defaults — New York style, zinc color, CSS variables)
npx shadcn@latest init -d

# 10. Install shadcn components
npx shadcn@latest add button card input label separator toast dialog dropdown-menu avatar badge

# 11. Initialize Supabase locally
npx supabase init

# 12. Start Supabase local instance
npx supabase start
```

### Phase 2: Create Directory Structure

```bash
# Create all directories
mkdir -p app/\(auth\)/login
mkdir -p app/\(auth\)/signup
mkdir -p app/auth/callback
mkdir -p app/\(dashboard\)
mkdir -p app/\(marketing\)
mkdir -p app/api/health
mkdir -p app/api/webhooks/stripe
mkdir -p app/api/cron
mkdir -p components/shared
mkdir -p lib/supabase
mkdir -p lib/gemini
mkdir -p lib/stripe
mkdir -p supabase/migrations
```

### Phase 3: Create Core Library Files

Create each file with the content from SKILL.md:

1. `lib/supabase/client.ts` — Browser client
2. `lib/supabase/server.ts` — Server client
3. `lib/supabase/middleware.ts` — Middleware session helper
4. `lib/gemini/client.ts` — Gemini API with model tiering
5. `lib/stripe/client.ts` — Stripe server client with helpers
6. `lib/utils.ts` — cn(), formatDate(), absoluteUrl()
7. `middleware.ts` — Root middleware with auth protection

### Phase 4: Create Pages

1. `app/layout.tsx` — Root layout with fonts, metadata, global providers
2. `app/globals.css` — Tailwind directives (generated by create-next-app, update as needed)
3. `app/not-found.tsx` — Custom 404 page
4. `app/(marketing)/page.tsx` — Landing page with hero, features, CTA
5. `app/(auth)/login/page.tsx` — Login form with email + Google OAuth
6. `app/(auth)/signup/page.tsx` — Signup form with email + Google OAuth
7. `app/auth/callback/route.ts` — OAuth callback handler
8. `app/(dashboard)/layout.tsx` — Dashboard layout with sidebar/nav
9. `app/(dashboard)/page.tsx` — Dashboard home page

### Phase 5: Create API Routes

1. `app/api/health/route.ts` — Health check endpoint
2. `app/api/webhooks/stripe/route.ts` — Stripe webhook handler
3. `app/api/cron/route.ts` — Cron job endpoint with auth

### Phase 6: Create Shared Components

1. `components/shared/navbar.tsx` — Top navigation bar

```tsx
import Link from "next/link";
import { Button } from "@/components/ui/button";

export function Navbar() {
  return (
    <header className="sticky top-0 z-50 w-full border-b bg-background/95 backdrop-blur">
      <div className="container flex h-16 items-center justify-between">
        <Link href="/" className="text-xl font-bold">
          YourApp
        </Link>
        <nav className="flex items-center gap-4">
          <Button variant="ghost" asChild>
            <Link href="/login">Sign in</Link>
          </Button>
          <Button asChild>
            <Link href="/signup">Get started</Link>
          </Button>
        </nav>
      </div>
    </header>
  );
}
```

2. `components/shared/footer.tsx` — Site footer
3. `components/shared/user-menu.tsx` — Authenticated user dropdown

### Phase 7: Database Setup

```bash
# Apply migrations
npx supabase db reset
```

This runs the migration in `supabase/migrations/00001_create_users.sql` which creates:
- `public.profiles` table with RLS
- Trigger to auto-create profile on auth signup
- Trigger to auto-update `updated_at`

### Phase 8: Configure Auth

Update `supabase/config.toml` to enable:
- Email auth with signups enabled
- Google OAuth (requires Google Cloud Console credentials)
- Redirect URLs pointing to `http://localhost:3000`

### Phase 9: Environment and Config Files

1. Create `.env.local.example` with all variables (see env-vars.md)
2. Copy to `.env.local` and fill in values from `npx supabase status`
3. Create `.prettierrc` with Tailwind plugin
4. Update `package.json` scripts to add `format` and `format:check`
5. Verify `.gitignore` includes `.env*.local`, `.supabase/`, `.vercel`

### Phase 10: Documentation

1. Create `README.md` with project overview, stack, getting started, structure
2. Create `IDEAS.md` with empty parking lot table

### Phase 11: Initial Commit and Deploy

```bash
# Format everything
npm run format

# Lint check
npm run lint

# Build check
npm run build

# Commit
git add -A
git commit -m "Initial project setup: Next.js + Supabase + Stripe + Gemini"

# Create GitHub repo and push
git branch -M main
gh repo create PROJECT_NAME --private --source=. --push

# Link Vercel
npx vercel link

# Deploy
npx vercel --prod
```

### Phase 12: Verification Checklist

- [ ] `npm run dev` starts on port 3000
- [ ] Landing page loads at `/`
- [ ] Login page loads at `/login`
- [ ] Signup page loads at `/signup`
- [ ] Dashboard redirects to `/login` when not authed
- [ ] `/api/health` returns `{ status: "ok" }`
- [ ] Supabase Studio accessible at `localhost:54323`
- [ ] `profiles` table visible in Supabase Studio
- [ ] `npm run build` succeeds
- [ ] `npm run lint` passes
- [ ] `npm run format:check` passes
- [ ] Vercel deployment is live

---

## Content Template

### Phase 1: Create and Configure Project

```bash
npx create-next-app@latest PROJECT_NAME \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --src-dir=false \
  --import-alias="@/*" \
  --use-npm

cd PROJECT_NAME
git init

npm install @supabase/supabase-js @supabase/ssr
npm install @next/mdx @mdx-js/loader @mdx-js/react gray-matter reading-time
npm install @google/generative-ai
npm install lucide-react class-variance-authority clsx tailwind-merge
npm install -D prettier prettier-plugin-tailwindcss @types/node @types/mdx

npx shadcn@latest init -d
npx shadcn@latest add button card separator badge

npx supabase init
npx supabase start
```

### Phase 2: Create Directory Structure

```bash
mkdir -p app/\(marketing\)
mkdir -p app/\(blog\)
mkdir -p app/\(blog\)/\[slug\]
mkdir -p app/api/health
mkdir -p app/api/cron
mkdir -p app/feed.xml
mkdir -p components/shared
mkdir -p components/blog
mkdir -p lib/supabase
mkdir -p lib/gemini
mkdir -p content/posts
```

### Phase 3: Create Core Library Files

1. `lib/supabase/client.ts` — Browser client
2. `lib/supabase/server.ts` — Server client
3. `lib/gemini/client.ts` — Gemini API client (use for AI-powered content tools)
4. `lib/utils.ts` — Utility functions
5. `lib/mdx.ts` — MDX processing:

```typescript
import fs from "fs";
import path from "path";
import matter from "gray-matter";
import readingTime from "reading-time";

const POSTS_DIR = path.join(process.cwd(), "content/posts");

export interface Post {
  slug: string;
  title: string;
  description: string;
  date: string;
  published: boolean;
  tags: string[];
  readingTime: string;
  content: string;
}

export function getAllPosts(): Post[] {
  const files = fs.readdirSync(POSTS_DIR).filter((f) => f.endsWith(".mdx"));
  
  return files
    .map((filename) => {
      const slug = filename.replace(".mdx", "");
      const filePath = path.join(POSTS_DIR, filename);
      const fileContents = fs.readFileSync(filePath, "utf-8");
      const { data, content } = matter(fileContents);

      return {
        slug,
        title: data.title,
        description: data.description,
        date: data.date,
        published: data.published ?? true,
        tags: data.tags ?? [],
        readingTime: readingTime(content).text,
        content,
      };
    })
    .filter((post) => post.published)
    .sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime());
}

export function getPostBySlug(slug: string): Post | undefined {
  return getAllPosts().find((post) => post.slug === slug);
}
```

### Phase 4: Create Pages

1. `app/layout.tsx` — Root layout with SEO-focused metadata
2. `app/(marketing)/page.tsx` — Landing page optimized for conversions
3. `app/(blog)/page.tsx` — Blog index with post cards:

```tsx
import { getAllPosts } from "@/lib/mdx";
import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import Link from "next/link";

export const metadata = {
  title: "Blog",
  description: "Latest posts and updates",
};

export default function BlogPage() {
  const posts = getAllPosts();

  return (
    <main className="container max-w-4xl py-12">
      <h1 className="mb-8 text-4xl font-bold">Blog</h1>
      <div className="space-y-6">
        {posts.map((post) => (
          <Link key={post.slug} href={`/${post.slug}`}>
            <Card className="transition-shadow hover:shadow-md">
              <CardHeader>
                <div className="flex items-center gap-2 text-sm text-muted-foreground">
                  <time>{post.date}</time>
                  <span>·</span>
                  <span>{post.readingTime}</span>
                </div>
                <CardTitle>{post.title}</CardTitle>
                <CardDescription>{post.description}</CardDescription>
                <div className="flex gap-2 pt-2">
                  {post.tags.map((tag) => (
                    <Badge key={tag} variant="secondary">{tag}</Badge>
                  ))}
                </div>
              </CardHeader>
            </Card>
          </Link>
        ))}
      </div>
    </main>
  );
}
```

4. `app/(blog)/[slug]/page.tsx` — Individual post page with metadata generation
5. `app/sitemap.ts` — Dynamic sitemap:

```typescript
import { getAllPosts } from "@/lib/mdx";
import { MetadataRoute } from "next";

export default function sitemap(): MetadataRoute.Sitemap {
  const baseUrl = process.env.NEXT_PUBLIC_APP_URL || "https://example.com";
  const posts = getAllPosts();

  const postEntries = posts.map((post) => ({
    url: `${baseUrl}/${post.slug}`,
    lastModified: new Date(post.date),
    changeFrequency: "weekly" as const,
    priority: 0.7,
  }));

  return [
    { url: baseUrl, lastModified: new Date(), changeFrequency: "daily", priority: 1 },
    { url: `${baseUrl}/blog`, lastModified: new Date(), changeFrequency: "daily", priority: 0.9 },
    ...postEntries,
  ];
}
```

6. `app/robots.ts` — Robots configuration
7. `app/feed.xml/route.ts` — RSS feed generator

### Phase 5: Create Sample Content

`content/posts/hello-world.mdx`:

```mdx
---
title: "Hello World"
description: "The first post on our new blog."
date: "2025-01-01"
published: true
tags: ["announcement"]
---

# Hello World

Welcome to the blog. This is your first post. Edit it or delete it and start writing.
```

### Phase 6: SEO Components

1. `components/blog/post-header.tsx` — Post title, date, reading time, tags
2. `components/blog/post-body.tsx` — MDX content renderer with styled prose
3. `components/shared/navbar.tsx` — Nav with blog link
4. `components/shared/footer.tsx` — Footer with sitemap links

### Phase 7: Environment, Config, Deploy

Same as SaaS Phase 9-12, minus Stripe variables.

### Verification Checklist

- [ ] Landing page loads at `/`
- [ ] Blog index loads at `/blog`
- [ ] Individual post loads at `/hello-world`
- [ ] `/sitemap.xml` returns valid XML
- [ ] `/robots.txt` returns valid robots file
- [ ] `/feed.xml` returns valid RSS
- [ ] `/api/health` returns `{ status: "ok" }`
- [ ] `npm run build` succeeds (static generation works)
- [ ] Vercel deployment is live

---

## Tool Template

### Phase 1: Create and Configure Project

```bash
npx create-next-app@latest PROJECT_NAME \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --src-dir=false \
  --import-alias="@/*" \
  --use-npm

cd PROJECT_NAME
git init

npm install @supabase/supabase-js @supabase/ssr
npm install @google/generative-ai
npm install lucide-react class-variance-authority clsx tailwind-merge
npm install -D prettier prettier-plugin-tailwindcss @types/node

npx shadcn@latest init -d
npx shadcn@latest add button card input label toast progress
npx supabase init
npx supabase start
```

### Phase 2: Create Directory Structure

```bash
mkdir -p app/\(tool\)
mkdir -p app/\(tool\)/results
mkdir -p app/\(marketing\)
mkdir -p app/api/health
mkdir -p app/api/analyze
mkdir -p components/shared
mkdir -p components/tool
mkdir -p lib/supabase
mkdir -p lib/gemini
```

### Phase 3: Create Core Library Files

1. `lib/supabase/client.ts` — Browser client
2. `lib/supabase/server.ts` — Server client
3. `lib/gemini/client.ts` — Gemini API client (primary intelligence for the tool)
4. `lib/utils.ts` — Utility functions
5. `lib/calculations.ts` — Core tool logic:

```typescript
// This file contains the core logic for your tool.
// Keep business logic separate from UI components.

export interface ToolInput {
  // Define the input shape for your tool
  data: string;
}

export interface ToolResult {
  // Define the output shape
  summary: string;
  score: number;
  details: Record<string, unknown>;
  createdAt: string;
}

export function processInput(input: ToolInput): ToolResult {
  // Implement your tool's core calculation/analysis here
  return {
    summary: "Analysis complete",
    score: 0,
    details: {},
    createdAt: new Date().toISOString(),
  };
}

// Generate a shareable ID for results
export function generateResultId(): string {
  return Math.random().toString(36).substring(2, 10);
}
```

### Phase 4: Create Pages

1. `app/layout.tsx` — Root layout, minimal and focused
2. `app/(marketing)/page.tsx` — Landing page explaining the tool
3. `app/(tool)/page.tsx` — Main tool interface:

```tsx
"use client";

import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Progress } from "@/components/ui/progress";

export default function ToolPage() {
  const [input, setInput] = useState("");
  const [loading, setLoading] = useState(false);
  const [progress, setProgress] = useState(0);

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setLoading(true);
    setProgress(30);

    const res = await fetch("/api/analyze", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ data: input }),
    });

    setProgress(80);
    const result = await res.json();
    setProgress(100);

    // Redirect to results page with the result ID
    window.location.href = `/results?id=${result.id}`;
  }

  return (
    <main className="container flex min-h-screen max-w-2xl flex-col items-center justify-center py-12">
      <Card className="w-full">
        <CardHeader>
          <CardTitle className="text-2xl">Your Tool Name</CardTitle>
        </CardHeader>
        <CardContent>
          <form onSubmit={handleSubmit} className="space-y-4">
            <div className="space-y-2">
              <Label htmlFor="input">Enter your data</Label>
              <Input
                id="input"
                value={input}
                onChange={(e) => setInput(e.target.value)}
                placeholder="Paste or type your input..."
                required
              />
            </div>
            {loading && <Progress value={progress} />}
            <Button type="submit" className="w-full" disabled={loading}>
              {loading ? "Analyzing..." : "Analyze"}
            </Button>
          </form>
        </CardContent>
      </Card>
    </main>
  );
}
```

4. `app/(tool)/results/page.tsx` — Shareable results page
5. `app/api/analyze/route.ts` — Analysis endpoint using Gemini
6. `app/api/health/route.ts` — Health check

### Phase 5: Database (optional for tool)

Only create if the tool needs to persist results for sharing:

```sql
-- supabase/migrations/00001_create_results.sql
create table public.results (
  id text primary key,
  input jsonb not null,
  output jsonb not null,
  created_at timestamptz default now() not null
);

-- Public read access for shared results
alter table public.results enable row level security;

create policy "Anyone can view results"
  on public.results for select
  using (true);

-- Only the API can insert (using service role key)
create policy "Service role can insert results"
  on public.results for insert
  with check (true);
```

### Phase 6: Environment, Config, Deploy

Same as SaaS Phase 9-12, minus Stripe variables.

### Verification Checklist

- [ ] Landing page loads at `/`
- [ ] Tool page loads at `/tool` (or wherever mapped)
- [ ] Form submits and shows progress
- [ ] Results page loads with shareable URL
- [ ] `/api/health` returns `{ status: "ok" }`
- [ ] `npm run build` succeeds
- [ ] Vercel deployment is live

---

## API Template

### Phase 1: Create and Configure Project

```bash
npx create-next-app@latest PROJECT_NAME \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --src-dir=false \
  --import-alias="@/*" \
  --use-npm

cd PROJECT_NAME
git init

npm install @supabase/supabase-js @supabase/ssr
npm install @google/generative-ai
npm install lucide-react class-variance-authority clsx tailwind-merge
npm install -D prettier prettier-plugin-tailwindcss @types/node

npx shadcn@latest init -d
npx shadcn@latest add button card badge

npx supabase init
npx supabase start
```

### Phase 2: Create Directory Structure

```bash
mkdir -p app/api/v1/example
mkdir -p app/api/health
mkdir -p app/api/webhooks/incoming
mkdir -p app/api/cron
mkdir -p components/shared
mkdir -p lib/supabase
mkdir -p lib/gemini
mkdir -p lib/validators
mkdir -p lib/queue
```

### Phase 3: Create Core Library Files

1. `lib/supabase/client.ts` — Browser client
2. `lib/supabase/server.ts` — Server client (primary for API routes)
3. `lib/gemini/client.ts` — Gemini API client
4. `lib/utils.ts` — Utility functions
5. `lib/validators/index.ts` — Request validation:

```typescript
import { NextResponse } from "next/server";

export class ValidationError extends Error {
  constructor(
    message: string,
    public statusCode: number = 400
  ) {
    super(message);
    this.name = "ValidationError";
  }
}

export function validateApiKey(request: Request): boolean {
  const apiKey = request.headers.get("x-api-key");
  return apiKey === process.env.API_SECRET_KEY;
}

export function requireApiKey(request: Request) {
  if (!validateApiKey(request)) {
    return NextResponse.json(
      { error: "Invalid or missing API key" },
      { status: 401 }
    );
  }
  return null;
}

export function validateBody<T>(body: unknown, requiredFields: string[]): body is T {
  if (!body || typeof body !== "object") return false;
  return requiredFields.every((field) => field in (body as Record<string, unknown>));
}

export function errorResponse(error: unknown) {
  if (error instanceof ValidationError) {
    return NextResponse.json({ error: error.message }, { status: error.statusCode });
  }
  console.error("Unhandled error:", error);
  return NextResponse.json({ error: "Internal server error" }, { status: 500 });
}
```

6. `lib/queue/index.ts` — Background job patterns:

```typescript
// Simple queue pattern using Supabase as a job store.
// For production, consider Inngest, Trigger.dev, or QStash.

import { createClient } from "@/lib/supabase/server";

export interface Job {
  id: string;
  type: string;
  payload: Record<string, unknown>;
  status: "pending" | "processing" | "completed" | "failed";
  created_at: string;
  processed_at: string | null;
  error: string | null;
}

export async function enqueueJob(type: string, payload: Record<string, unknown>) {
  const supabase = await createClient();
  const { data, error } = await supabase
    .from("jobs")
    .insert({ type, payload, status: "pending" })
    .select()
    .single();

  if (error) throw error;
  return data as Job;
}

export async function processNextJob(type: string): Promise<Job | null> {
  const supabase = await createClient();

  const { data: job, error } = await supabase
    .from("jobs")
    .update({ status: "processing" })
    .eq("type", type)
    .eq("status", "pending")
    .order("created_at", { ascending: true })
    .limit(1)
    .select()
    .single();

  if (error || !job) return null;
  return job as Job;
}

export async function completeJob(id: string) {
  const supabase = await createClient();
  await supabase
    .from("jobs")
    .update({ status: "completed", processed_at: new Date().toISOString() })
    .eq("id", id);
}

export async function failJob(id: string, error: string) {
  const supabase = await createClient();
  await supabase
    .from("jobs")
    .update({ status: "failed", error, processed_at: new Date().toISOString() })
    .eq("id", id);
}
```

### Phase 4: Create API Routes

1. `app/api/health/route.ts` — Health check
2. `app/api/v1/example/route.ts` — Example versioned endpoint:

```typescript
import { NextResponse } from "next/server";
import { requireApiKey, validateBody, errorResponse } from "@/lib/validators";

interface ExampleRequest {
  message: string;
}

export async function GET(request: Request) {
  const authError = requireApiKey(request);
  if (authError) return authError;

  return NextResponse.json({
    data: [],
    meta: { total: 0, page: 1, per_page: 20 },
  });
}

export async function POST(request: Request) {
  const authError = requireApiKey(request);
  if (authError) return authError;

  try {
    const body = await request.json();
    if (!validateBody<ExampleRequest>(body, ["message"])) {
      return NextResponse.json({ error: "Missing required field: message" }, { status: 400 });
    }

    return NextResponse.json({ data: { message: body.message, processed: true } }, { status: 201 });
  } catch (error) {
    return errorResponse(error);
  }
}
```

3. `app/api/webhooks/incoming/route.ts` — Generic webhook receiver:

```typescript
import { NextResponse } from "next/server";
import { enqueueJob } from "@/lib/queue";

export async function POST(request: Request) {
  try {
    const body = await request.json();

    // Log the webhook for debugging
    console.log("Webhook received:", JSON.stringify(body).substring(0, 200));

    // Enqueue for background processing
    const job = await enqueueJob("webhook_processing", body);

    return NextResponse.json({ received: true, jobId: job.id });
  } catch (error) {
    console.error("Webhook error:", error);
    return NextResponse.json({ error: "Processing failed" }, { status: 500 });
  }
}
```

4. `app/api/cron/route.ts` — Cron endpoint with auth

### Phase 5: Create Status Page

`app/page.tsx` — Minimal API status page:

```tsx
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function StatusPage() {
  return (
    <main className="container flex min-h-screen max-w-2xl flex-col items-center justify-center py-12">
      <Card className="w-full">
        <CardHeader>
          <div className="flex items-center justify-between">
            <CardTitle>API Status</CardTitle>
            <Badge variant="default">Operational</Badge>
          </div>
        </CardHeader>
        <CardContent className="space-y-4">
          <div>
            <h3 className="font-semibold">Endpoints</h3>
            <ul className="mt-2 space-y-1 text-sm text-muted-foreground">
              <li><code>GET /api/health</code> — Health check</li>
              <li><code>GET /api/v1/example</code> — List resources</li>
              <li><code>POST /api/v1/example</code> — Create resource</li>
              <li><code>POST /api/webhooks/incoming</code> — Webhook receiver</li>
              <li><code>GET /api/cron</code> — Scheduled tasks</li>
            </ul>
          </div>
          <div>
            <h3 className="font-semibold">Authentication</h3>
            <p className="mt-1 text-sm text-muted-foreground">
              All API endpoints require an <code>x-api-key</code> header.
            </p>
          </div>
        </CardContent>
      </Card>
    </main>
  );
}
```

### Phase 6: Database Setup

```sql
-- supabase/migrations/00001_create_jobs.sql
create extension if not exists "uuid-ossp";

create table public.jobs (
  id uuid default uuid_generate_v4() primary key,
  type text not null,
  payload jsonb not null default '{}',
  status text not null default 'pending' check (status in ('pending', 'processing', 'completed', 'failed')),
  error text,
  created_at timestamptz default now() not null,
  processed_at timestamptz
);

create index idx_jobs_status_type on public.jobs (status, type);
create index idx_jobs_created_at on public.jobs (created_at);

-- RLS: only service role can access jobs
alter table public.jobs enable row level security;
```

### Phase 7: Environment, Config, Deploy

Same as SaaS Phase 9-12, minus Stripe variables. Add `API_SECRET_KEY` to env vars.

### Verification Checklist

- [ ] Status page loads at `/`
- [ ] `/api/health` returns `{ status: "ok" }`
- [ ] `/api/v1/example` returns 401 without API key
- [ ] `/api/v1/example` returns 200 with valid API key
- [ ] `/api/webhooks/incoming` accepts POST requests
- [ ] `/api/cron` returns 401 without Bearer token
- [ ] Jobs table exists in Supabase
- [ ] `npm run build` succeeds
- [ ] Vercel deployment is live
